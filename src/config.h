#ifndef CONFIG_H
#define CONFIG_H

namespace Globals {
    static const auto isotope_source_most_recent = "isotope_source_most_recent";
    static const auto isotope_source_data_base_path = "isotope_source_data_base_path";
    static const auto test_script_path_settings_key = "test script path";
    static const auto device_protocols_file_settings_key = "device protocols path";
    static const auto rpc_xml_files_path_settings_key = "rpc xml files path";
    static const auto lua_editor_path_settings_key = "lua editor path";
    static const auto lua_editor_parameters_settings_key = "lua editor parameters";
    static const auto measurement_equipment_meta_data_path = "measurement equipment meta data path";
    static const auto last_csv_saved_directory = "last csv saved directory";
    static const auto form_directory = "form directory";
    static const auto form_definitions_directory = "forms definitions directory";
    static const auto git_path = "git_path";
    static const auto confirm_key_sequence = "confirm key sequence";
    static const auto skip_key_sequence = "skip key sequence";
    static const auto cancel_key_sequence = "cancel key sequence";
}

#endif // CONFIG_H
